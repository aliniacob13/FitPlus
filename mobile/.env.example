# Backend URL (must include /api/v1). On a physical phone with Expo Go, use your PC's LAN IP — not localhost.
# Example: http://192.168.1.15:8000/api/v1 (same Wi‑Fi as the phone). Rebuild/restart Expo after changing.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.15:8000/api/v1
